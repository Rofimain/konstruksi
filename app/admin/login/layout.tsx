// app/admin/login/layout.tsx
// Layout khusus login — tidak pakai navbar/footer website, tidak pakai admin sidebar
export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
